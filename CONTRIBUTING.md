# Contributing

This project welcomes contributions and suggestions.

## How do I build and run the plugin locally?

### Requirements

* JDK 21

### Building the plugin and launching RustRover in a sandbox

```bash
cd gdscript
./gradlew runRustRover
```

### Running tests

```bash
cd gdscript
./gradlew test --console=plain --no-daemon
```
